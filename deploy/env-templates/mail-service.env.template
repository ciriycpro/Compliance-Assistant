# /etc/mail-stack/mail-service.env

# JSON array: [{label, host, port, user, pass, mailbox}, ...]
MAILBOXES_JSON=[{"label":"<NAME>","host":"imap.example.com","port":993,"user":"<EMAIL>","pass":"<APP_PASSWORD>","mailbox":"INBOX"}]

# Legacy single-mailbox (deprecated, use MAILBOXES_JSON)
IMAP_HOST=
IMAP_PORT=993
IMAP_USER=
IMAP_PASS=
